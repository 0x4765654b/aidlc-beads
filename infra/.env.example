# Gorilla Troop -- Environment Configuration
# Copy this file to .env and fill in your values.

# ── AWS Credentials (for Bedrock / Claude) ────────────────────────
# Option A (recommended): Use an AWS CLI named profile
AWS_PROFILE=ai3_d
AWS_DEFAULT_REGION=us-east-1

# Option B: Explicit credentials (uncomment if not using a profile)
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# ── Bedrock Model Configuration ──────────────────────────────────
BEDROCK_MODEL_ID=us.anthropic.claude-opus-4-6-v1
BEDROCK_REGION=us-east-1

# ── Port Configuration ────────────────────────────────────────────
ORCHESTRATOR_PORT=9741
DASHBOARD_PORT=3000

# ── Project Workspace ─────────────────────────────────────────────
# Path to the directory containing your AIDLC project workspaces
PROJECT_WORKSPACE=./workspace

# ── Outline Wiki ──────────────────────────────────────────────────
OUTLINE_SECRET_KEY=generate-with-openssl-rand-hex-32
OUTLINE_UTILS_SECRET=generate-with-openssl-rand-hex-32
OUTLINE_DB_PASSWORD=outline
OUTLINE_URL=http://localhost:3001

# ── Agent Mail ────────────────────────────────────────────────────
AGENT_MAIL_URL=http://agent-mail:8765
