# ── Gorilla Troop Environment Configuration ─────────────────────────
# Copy to .env and adjust values for your environment:
#   cp .env.example .env

# ── AWS Credentials (profile-based) ─────────────────────────────────
# Path to your .aws config directory on the HOST.
# Docker Desktop runs as a Windows process, so use Windows paths here.
#   Windows example:  C:\Users\yourname\.aws
#   WSL2 example:     /mnt/c/Users/yourname/.aws
AWS_CONFIG_DIR=C:\Users\yourname\.aws
AWS_PROFILE=ai3_d
AWS_DEFAULT_REGION=us-east-1

# ── Bedrock Model ───────────────────────────────────────────────────
BEDROCK_MODEL_ID=us.anthropic.claude-opus-4-6-v1
BEDROCK_REGION=us-east-1

# ── Workspace Root ─────────────────────────────────────────────────
# Host path to the directory containing your project repos.
# Docker Desktop runs as a Windows process, so use Windows paths here.
# This is bind-mounted to /workspace inside the container.
# Each project repo (e.g. aidlc-beads) becomes /workspace/<repo>.
PROJECT_WORKSPACE=C:\dev\git-repos

# ── Container Paths ────────────────────────────────────────────────
# Where the orchestrator stores its registry (.gorilla-troop/).
# Defaults to /workspace -- persists to PROJECT_WORKSPACE on the host.
GORILLA_WORKSPACE=/workspace

# ── Default Project (auto-registered on startup) ───────────────────
# Container-side path to the default project within /workspace.
DEFAULT_PROJECT_KEY=aidlc-beads
DEFAULT_PROJECT_NAME=AIDLC Beads
DEFAULT_PROJECT_PATH=/workspace/aidlc-beads

# ── Service Ports ──────────────────────────────────────────────────
ORCHESTRATOR_PORT=9741
DASHBOARD_PORT=3000

# ── Agent Mail ─────────────────────────────────────────────────────
AGENT_MAIL_URL=http://agent-mail:8765

# ── Outline Wiki (optional -- only needed with --profile outline) ──
OUTLINE_SECRET_KEY=change-me-generate-with-openssl-rand-hex-32
OUTLINE_UTILS_SECRET=change-me-generate-with-openssl-rand-hex-32
OUTLINE_DB_PASSWORD=outline
OUTLINE_URL=http://localhost:3001
