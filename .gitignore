# Beads: .beads/.gitignore handles ignoring SQLite DBs, lock files, etc.
# Portable files (issues.jsonl, config.yaml, metadata.json) are tracked.
.beads/*.tmp.*
.beads/.migration-hint-ts


# Gorilla Troop registry (auto-managed by orchestrator)
.gorilla-troop/

# IDE and editor config (user-specific)
.claude/
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Node.js (if applicable)
node_modules/

# Outline (self-hosted review UI)
outline/.env
outline/docker-compose.override.yml

# Infrastructure secrets and binaries
infra/.env
infra/bin/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Build artifacts (project-specific -- extend as needed)
dist/
build/
out/
